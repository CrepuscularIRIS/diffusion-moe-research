# ============================================================
# Diffusion MoE research repo — strict ignore.
# Only research CODE + plan docs are tracked. NEVER `git add -A`.
# ============================================================

# --- Secrets (NEVER commit) ---
.env
.env.*
*.key
*.pem
credentials*.json
*_secret*
*_token*
.claude/
.antigravitycli/

# --- Model weights / large binaries ---
*.safetensors
*.pt
*.pth
*.bin
*.gguf
*.ckpt
*.onnx
*.npz

# --- Large workspace dirs (top-level only; anchored with leading /) ---
/models/
/data/
/repos/
/distill_audit/
/mindset/
/unsloth_compiled_cache/

# --- Nested git projects (versioned separately) ---
/cot-distillation-fidelity-audit/
/Jackrong-llm-finetuning-guide/

# --- VLA side project (nested repos + large datasets/checkpoints, not tracked here) ---
/VLA/

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/
.venv/
venv/

# --- Tooling working dirs ---
/.playwright-mcp/
# Browser scratch captures saved to repo root
/*.png
/page-*.yml
/console-*.log

# --- Run artifacts / Arbor session state ---
/.arbor/
outputs/
checkpoints/
wandb/
runs/
*.log
*.output
nohup.out

# --- Prior-art PDFs + closed He-line scratch code (not tracked) ---
/plan/priorart_pdfs/
*.pdf
/diffusiongemma_sft/heline/
plan/Audit/backup-pre-atlas-2026-07-04/

# Showcase site — standalone Vite app with its own git; not part of the research repo
/UI/site/
