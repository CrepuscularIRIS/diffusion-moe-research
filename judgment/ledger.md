# Judgment Ledger — append-only forecast log (LIVE: open rows only)

> Operations = the research-os `ledger` skill (WRITE / RESOLVE / DISTILL / APPLY / RECALL + curation).
> Rows are written BEFORE outcomes are knowable; a post-hoc edited row is VOID. Open rows resolve at the
> 验收 their resolver names. Scores: Brier for probabilities; log-ratio error for numeric estimates.
> Resolved + distilled rows move VERBATIM to `ledger-archive.md`.
>
> **FRESH RESTART 2026-07-13:** the WORLD MODELS direction was restarted from scratch (Arbor run
> `world-model-v2`, re-ideate from zero). All prior rows J1–J12 are in `ledger-archive.md` — read the
> archive before re-litigating a dead slice. The methodological priors L1–L10 in `lessons.md` CARRY OVER
> (they are portable research judgment, not campaign state). No open rows yet — the first is written at
> the first claim-bearing `/prereg` of the fresh campaign.

| id | date | type | the call | forecast | basis | resolver | outcome | score | lesson |
|---|---|---|---|---|---|---|---|---|---|
| W13 | 2026-07-15 | HUMAN-STEER (record-only → research-os v4.6.1) | **Sub-skill invocation made procedural, not associative.** User audit: the loop "never uses" `abduce`/`externalize`/`ledger`/`operate`/`tricks` — but grep proved they are densely wired (conductor §5 + cross-refs) and fresh (all v4.6.0). Root cause diagnosed = neither staleness nor orphaning but **autonomous-loop triggering**: in a `/goal` run the conductor is the only skill loaded, and it merely *named* sub-skills in prose (associative cue), so the model improvised their behaviour from one-line summaries instead of loading the real mechanics (Brier scoring, diagnostic palette, abduction protocol, launch arithmetic) via the Skill tool. Also cleared: `dispatching-parallel-agents` is a superpowers skill, NOT research-os. FIX (both mechanisms, user chose "harden conductor AND embed in goal"): (1) conductor §5 upgraded from prose list → **BINDING invocation manifest** — "when the loop hits a trigger, your FIRST action is to invoke the Skill tool for that skill", trigger→load table; §0 header note that "recall/externalize/operate" mean *invoke that skill*. (2) goal-directive gains a compact **pointer manifest** (triggers→loads, explicitly NOT restated mechanics — DRY preserved, honors the two-line-contract ruling; rejected embedding skill CONTENT into the goal as it re-grows the fat manual). Separately verified: **codebase-memory MCP is unused** (memory.db = 0 rows since 2026-07-12, zero skill invocations, sole ref = one dangling CLAUDE.md line) — safe to uninstall; removal of the dangling line offered, pending. | — | user directive; wiring + DB-emptiness verified by grep/sqlite before prescribing | — | — | — | a skill NAMED in prose ≠ a skill INVOKED — in an autonomous loop, sub-skill loads must be procedural imperatives on the one always-loaded skill, not associative description-matches |
| W12 | 2026-07-15 | HUMAN-STEER (record-only, amends W11 + form pass → research-os v4.6) | (1) **`sol` lane LIVE via codex** (user: "Codex with GPT-5.6 Sol High, run analyses in parallel"): `moa_ask.sh sol` = `codex exec -m gpt-5.6-sol -c model_reasoning_effort="high"` (env `MOA_SOL_MODEL`/`MOA_SOL_EFFORT`) — VERIFIED: session header reports model+effort, invalid `-m` errors (flag honored), 2-instance parallel fan-out tested through the script. W11's "sol dormant / interim codex-charter" workaround DISSOLVED — the dissection arm IS codex-Sol; zen `brain` agent stays a dormant alternative substrate; fallback = fresh browser chats. Same tool caveat as gpt5 (codex retains shell/MCP). (2) **Skills streamlined (all 13 → v4.6.0):** every description rewritten TRIGGER-FIRST ("Use when <situations>. Keywords — … . <compressed mechanics>. DRIFT-TRIGGER…") for natural-language invocation; verbose bodies tightened (Mine 7 12→7 lines, DISSECTION-MODE 23→15, external-brain role 3, forge step-1 locus, autopsy extraction row); abduce mandatory-mechanism list gains **the algorithmic cause** (3rd mandatory, closing the last dual-path audit gap). Net: dual-path+dissection machinery held at ~+28 total skill lines vs pre-v4.5. Runtime synced, live-verified. | — | user directive; codex model override verified empirically before binding | — | — | — | bind lanes to verified substrates; triggers belong at the FRONT of a description |
| W11 | 2026-07-14 | HUMAN-STEER (record-only, extends W10) | DISSECTION directive: algorithm critique = a selectable prospecting mode executed by parallel GPT-5.6 Sol instances (experienced-algorithm-engineer stance) + the MoA panel, reading implementation + papers, reconciled into weaknesses/counterexamples/directions — with an explicit ANTI-SHALLOW discipline (critique must anchor to equations/code/assumptions/target-distribution; unrewarded criticism dies at reconcile). Shipped: moa-routing **DISSECTION-MODE** (cold DISSECTION PACKET mandatory — verbatim equations · code seams file:line · assumptions · target distribution; per-lane contract WEAKNESS(anchored)/FAILURE-HYPOTHESIS(regime+magnitude)/MINIMAL-COUNTEREXAMPLE/DISCRIMINATING-PROBE/SELF-ATTACK; hallucinated anchor voids the lane; STAKES test at reconcile); prospect Mine 7 fires it at scale; external-brain role 3 parallel-instances clause. Binding (router-protocol §Dissection): PRIMARY = `sol` lane N-parallel (gpt-5.6-sol, DORMANT — zen billing unfunded; wakes with no protocol change); INTERIM = fresh browser-GPT-5.6 chats (context-free, serial) + parallel `codex exec` (charter judgment: structural-weakness hunting = forensic work, so DISSECTION is the one non-autopsy context where codex fans out — never a reconciling panel seat, never sets direction). Gap review (2 real, fixed): (1) delegated math was UNVERIFIED → forge: cross-family CHECK before claim-bearing prereg (deriver never checks its own math); (2) forge locus fork widened to tri-locus object\|algorithm\|DATA. Left emergent by design: measurement/argumentation banks, benchmark-creation (Evaluation type + Mine 5 cover it), task-reformulation (Mine 6/Novelty cover it). | — | user directive; architectural judgment per no-clarification instruction | — | — | — | grounded adversarial dissection; critique ≠ contribution until anchored + staked |
| W10 | 2026-07-14 | HUMAN-STEER (record-only) | DUAL-PATH ruling: the pipeline over-invested in MODELING-OBJECT shifts as the source of novelty and under-weighted the ALGORITHM path (estimator design · objective mismatch · optimization dynamics/credit · information flow · approximation validity · shift behavior). Two audits confirmed: bias real at the GENERATIVE layer (no mine/mode/anchor/type seeded from the incumbent algorithm's mechanism; moa chain forced object-shift as the only legal rewrite; autopsy ladder could only weaken claims, never repair mechanisms; forge's delegated-math clause pointed at a nonexistent external-brain contract; admit-check-3+L10+anchor-B4 formed an "index/object" reinforcing loop all minted from ONE campaign failure) while gates were already framing-neutral. Shipped as research-os v4.5: prospect Mine 7 INCUMBENT-ALGORITHM AUTOPSY + WHY-NOW widened; research-types + Method-repair 机理修复 row (diagnosis×fix one unit; ablation restores failure); forge step-1 LOCUS fork (object vs algorithm) + regeneration-rule locus branch; autopsy extraction-row `failure-locus` field + SAME-OBJECT ALGORITHM FIX as first ladder rung; atlas locus audit column; moa-routing ALGORITHM-MODE (fixed object, N parallel algorithmic hypotheses) + stage-localization escape hatch; external-brain role 3 algorithm-audit/derivation arm (GPT-5.6 = deep algorithm solver — closes the dangling delegation); tricks +3 training-side symptom rows; operator banks (taste-operators + opus-pass charter) widened to algorithm-path moves (axis-4 widening, not a 5th axis); admit check-3 accepts {mechanism-quantity+fix} pair; taste anchor G4 (Double Q-learning diagnose→minimal-fix); L10 algorithm-path corollary. J6's decoupling finding = the canonical mis-metabolized algorithm-locus signal. | — | user directive + dual parallel audits (SELECT + SOLVE loops) | — | — | — | dual-path: modeling and algorithm are co-equal research paths |
| B1 | 2026-07-14 | PREREG/REPLICATION (OPEN — Phase-1 DONE, Phase-2 pending) | Consistency-collapse phenomenon (accuracy–robustness inversion + proposal-evolution locus via teacher-force rescue) REPLICATES on ≥3 of 4 NEW distinct morphology families beyond walker/cheetah (human-approved breadth, Option A). Contract: `openbuild/world-model/prereg/breadth-replication-2026-07-14.md`. **Phase-1 (2026-07-15): hopper-hop REPLICATES (mass2x: collapse=0.042 rescue=1.885; actuator0.5x: collapse=0.057 rescue=1.068); quadruped-walk DOES-NOT-REPLICATE (actuator0.5x collapse_ratio=0.803, not inverted). 1/2 Phase-1 tasks REPLICATES → Phase-2 proceeds.** | **0.55** | mechanism (proposal-evolution instability of terminal-value-guided sampling planners) is condition-general → above a fresh-build prior; BUT substrate-dependence real (L7: cheetah-grav2× already graceful; shift may be dynamics-robust on some morphologies; manipulation families may not host it). NOT L1's 0.3 floor (replication of a MEASURED mechanism, not an unproven build). | Phase-1 + Phase-2 replication 验收 across families (PHASE-1 COMPLETE 2026-07-15; Phase-2 = ≥2 of {finger/reacher manipulation · fish-swim · acrobot} to reach ≥3/4 new families bar) | — | — | L7·L1(floor-lift) |
| W9 | 2026-07-14 | HUMAN-STEER (record-only, corrects W8) | OpenCode-MoA SCOPE directive: the OpenCode-MoA layer carries ONLY the three OpenCode-Go models (deepseek-v4-pro · mimo-v2.5-pro · qwen3.7-plus); no other models on OpenCode. **kimi (`opencode-go/kimi-k2.6`) REMOVED from the layer** — reverses the W8 "kimi = 6th family" addition. Opus 4.6 stays on `claude -p`, Gemini 3.1 Pro on `agy` — native CLIs, never folded into the OpenCode runtime (deliberate separation, not a stopgap). Applied: kimi out of `moa_ask.sh`/`moa_ping.sh`/`moa_jury.sh`/roster README + all bindings (router-protocol, CLAUDE.md §2, memory, plan). Consequences: panel = 5 families (kimi-not-in-`moa_panel.sh` now the CORRECT state, not a gap); jury = 4 non-Claude families {deepseek qwen mimo gemini}, quorum lowered 4→3 (`MOA_JURY_QUORUM`) so one dead lane (gemini/agy is flakiest) can't void the jury; jury stances now 1:1 with 4 lanes. Also fixed (audit): librarian SCHOLAR-QUERIES emit aligned to the marker-line contract (was a code-fence, contradicting moa_lit.sh/router-protocol/memory); dead `gv()`/`ENV_FILE` dropped from `moa_ask.sh`. Codex (forensic lane, walled off panel+jury), Chrome two-tab, Scholar handoff audited — clean. | — | user directive; separation-of-runtimes design (native CLIs ≠ OpenCode substrate) | — | — | — | scope discipline: no model drift into a substrate |
| W8 | 2026-07-14 | HUMAN-STEER (record-only) | OpenCode-MoA migration directive: advisors/jury/literature move onto OpenCode agents as named roles with maintained system prompts (roster `~/cli/opencode-moa`); the SELECTION JURY moves out of research-os process text into the MoA layer as INFRASTRUCTURE — moa-routing JURY-MODE stays the protocol authority, only the EXECUTION substrate moved; `claude -p`/codex reserved for artifact-grounded due-process reviews (PaperJury); pipeline stays de-gated + minimally intrusive. Shipped (T1–T5): advisor substrate `mode: primary` + ALL tools permission-denied (denial VERIFIED live) → mimo/deepseek/qwen honesty now agent-level-enforced, denial VERIFIED live; kimi-k2.6 = NEW 6th panel family (quorum resilience); `moa_jury.sh` (5 non-Claude jurors, quorum ≥4, mechanical tally; toy run 5/5 families, anchor-bad card zero picks); `moa_lit.sh` librarian lane (structured cards + SCHOLAR-QUERIES handoff) supersedes moa_search.sh; dormant zen lanes (sol/brain · forensic gateway twin · gemini/opus46 tool-free upgrade = billing flip). T6 = bindings sync (this row); user deep-fix pass on lane scripts pending. | — | teaching contract; research-os skills zero-edited (verified zero lane-specific references — bindings-only design held) | — | — | — | tool-substrate consolidation |
| W7 | 2026-07-14 | HUMAN-STEER (record-only) | De-gating + delegation ruling → research-os v4.4: (1) DELEGATED MODELING — the conductor never authors the mathematics; briefs go to MoA/external brain, conductor reconciles (conductor §1 + engine table + forge step 3); (2) PERSISTENCE — documents are not progress; rulings implemented in-session; blocking artifacts become in-flight (admit ack async, prospect ends with probes in flight, adversary-clear ≠ campaign-done); (3) devil's-advocate gate audit (evidence: every gate passed the W5-ruled-hollow work; only load-bearing catch = independent adversary) → admit 6 checks → pricing line + kill-first-3 + venue review; prereg −4 process-meta fields; prospect refine folded into jury; ≤12-cycle cap → evidence-driven stopping; autopsy ladder venue-review priced-optional below claim stakes (touches the 2026-07-13 value-bar ruling — ladder+floor+ceiling intact); ~duplication sweep. (4) prospect Mine 4 now RETRIEVED-never-remembered (breakthrough corpus via literature lane). Jury-hosting evaluation delivered (recommend: keep jury on OpenCode-MoA; user handles install). | — | teaching contract; audit = pipeline-devils-advocate pattern, gate-pressure check firing as designed | — | — | — | gate-creep prune |
| W6 | 2026-07-14 | HUMAN-STEER (record-only, refines W5) | Precision refinement: status = EVIDENCE-COMPLETE not contribution-complete (the work is NOT hollow of content — the exclusion characterization is real; what's missing is the positive object). Corrections applied: draft §5 "NOW BUILT" contradiction fixed; "consistency is the isolated cause" downgraded to "ranking discrepancy = remaining mechanism most consistent with evidence tested"; all-evidence-is-post-hoc named. Program v2 = contribution triple {index (CI_rank elite-weighted + CI_proposal/D_update) · held-out collapse detector · Consistency-Guarded MPPI (trust-region β, degeneracy check)} + rank-distilled-critic sufficiency test + four Go/No-Go gates. System: EVIDENCE/CONTRIBUTION-COMPLETE split → conductor; deliverable = contribution-complete package (goal-directive); writing bar P≥~0.85; PaperJury dual review channels bound + due-process contract absorbed into moa-routing. | — | teaching contract | — | — | — | L10 (refined) |
| W5 | 2026-07-14 | HUMAN-STEER (record-only, never scored) | Human ruled the characterization DRAFT below the top-tier bar: "refined but hollow" — consistency eliminated-against-confounds but never DEFINED (no formula/metric/decision rule ⇒ no leverage). Redirect = the consistency-INDEX program: define (CI-1 rank-consistency · CI-2 learned-model Bellman residual) → retrodict the existing λ-sweep/decoupling/graceful-cell grid → predict unseen cells (five-minute test) → sufficiency construction (wrong-but-consistent critic; check the W2 affine-scale arm FIRST) → index-derived mitigation + interface taxonomy. Directive `plan/wm-consistency-index-directive-2026-07-14.md`. | — | teaching contract: principle extracted → lessons L10 + taste-anchor B4; peer review at draft scale rebound to multi-family + literature-grounded | — | — | — | L10 (new) |
| W4 | 2026-07-14 | ADVERSARY/FACTORIAL | Mitigation B survives the 6-arm falsification as a GENUINE contribution: B > frozen-argmax AND > naive-blend AND > 2×-budget (accurate final-selection adds value beyond selection-hygiene) AND true-propose-accurate re-collapses (mechanism confirmed) | **0.30** (B-genuine) · **0.85** (arm-5 re-collapses = mechanism holds) | Grok r5 caught B confounds gumbel→argmax + frozen→MC; my OWN OBS2 showed the accurate critic mis-selects only 1.1× on a fixed bank → its added value AT SELECTION is small → B's win is likely mostly argmax>sampling (selection hygiene), NOT the accurate critic. Mechanism (arm 5 collapses) is well-isolated → high. B-as-contribution = genuinely at risk. | factorial 验收 (node 5.9) | **B-genuine = NO** (walker-grav2x: B∈CI of naive-blend +27.7[-52,+107] and 2x-budget +30.2[-24,+94] → selection-hygiene, not the accurate critic); **arm5-collapse = YES** (propose-accurate collapses all 3 cells 53/70/0 → mechanism confirmed). Cross-task unsettled (blend/2x not run on mass2x/cheetah). | B-genuine **0.09** (0.30→NO, well-calibrated LOW; my own OBS2 flagged the accurate-critic-adds-little-at-selection risk) · arm5 **0.02** | **baseline-fairness: a fix that changes 2 knobs at once (selection-rule + critic) needs the isolating factorial; the independent adversary caught it, I didn't. L5 confirmed (kills>builds — the mitigation build deflated, the mechanism kill stood).** |
| W3 | 2026-07-14 | PREREG/MITIGATION | AAAI mitigation: a consistency-preserving adaptation (JOINT model+value, or proposal-anchored planner) ≥ frozen under shift (avoids the collapse, accurate-value safely usable) — the actionable bar; STRETCH: > frozen (recovers toward nominal) | **0.55** (avoids-collapse ≥frozen) / **0.30** (stretch >frozen) | mechanism strongly predicts JOINT keeps model-value consistency by construction → likely avoids collapse; but L2 (first recipe cap 0.4) + the campaign's repeated finding that adaptation-under-shift is hard (single-component all failed) tempers the STRETCH; condition-E (true-dyn+frozen-val was WORSE) warns even consistent adaptation may not beat frozen. | mitigation closed-loop 验收 (node 5.9) | **YES (both)** — MIT-B (propose-frozen/select-accurate) 482 > frozen 439 (Δ+42.6 CI[+0.9,+88]), zero retraining; MIT-A joint 134 escapes collapse (partial). Primary AND stretch met. | primary 0.20 · stretch **0.49** (under-forecast) | **L2-refinement: a mechanism-DERIVED method (B operationalizes the isolated proposal-evolution mechanism) deserves a higher prior than L2's 0.4 UN-grounded-first-recipe cap** — I priced the stretch 0.30, it hit. |
| W2 | 2026-07-13 | SELECT/ADMIT | "Terminal-Value Compatibility Cliffs" direction: multi-shift TD-MPC2 replication (kill-first #1) confirms the α-cliff + search-feedback-rescue holds across ≥3 shift regimes (not grav2x-walker-specific) | **0.60** | phenomenon robust to every control run (temperature, affine-scale, static-ranking, α-interp, crossover); mechanism clean+identified; L1 floor LIFTED (measured evidence, not no-evidence); main risk = grav2x-specificity but the mechanism (MPPI value-perturbation search-instability) is condition-general. L5: BUILD/characterization bet, price cautiously. | multi-shift replication 验收 (node 5.3) | **YES** — cliff+collapse replicate 3 shift types (grav2x/mass2x/actuator0.5x, collapse-ratio 0.05-0.13) + crossover-rescue replicates on mass2x (508≈518). NOT grav2x-specific. Caveat: single-task (only walker ckpt). | **0.16** (correct, appropriately uncertain) | L1(floor-lift confirmed)·L5 |
| W1 | 2026-07-13 | SELECT/PROBE | Card A rung1: closed-loop LEARNED value-adaptation (J01) recovers meaningful return @grav2x AND beats dynamics-adaptation (J10) on frozen TD-MPC2 walker | **0.20** | v2 cheap probe REVERSED the pro-value signal (staleness = decision-inert level-bias, regret_V_norm 0.34 ≤ regret_M_norm 0.34, τ_retest 0.26); L2 first-recipe cap 0.4 → priced DOWN to 0.2 (measured evidence leans negative); L1 (no survival >0.3 w/o positive measured evidence). Hard-core corollary MANDATES the confirmatory closed-loop probe despite low P. | rung1 closed-loop return 验收 (node 1.1) | **NO — value-adaptation CATASTROPHICALLY HURT** (MC-fit critic, firing clean Q 188→77.9 ✓; J01-MC 31.7 vs J00 440.1, ΔJ01=−408.5 CI[−451,−357]; dynamics J10 also null −26). Single-component TTA breaks model-value consistency. | **0.04** (well-calibrated low; predicted inert, actual destructive — even more negative) | L2·L5·**new L→co-calibration** |
