# Judgment Ledger — ARCHIVE (resolved + distilled rows, moved verbatim)

> Append-only integrity preserved: rows move here UNEDITED once (a) resolved + scored and (b) their
> lesson distilled into `lessons.md`. The live `ledger.md` holds open rows only. Curation per the
> research-os `ledger` skill (2026-07-12).

| id | date | type | the call | forecast | basis | resolver | outcome | score | lesson |
|---|---|---|---|---|---|---|---|---|---|
| J1 | 2026-07-12 | OCCUPANCY | Adaptive imagination horizon is SATURATED (Neubay 2512.04341 + ELVIS 2605.04709 occupy it) — the CAWM-H kill was correct | p=0.85 {inferred} | two independent audits converge on the same two papers; citations are future-dated triage-grade, unread locally (L4 caps at 0.85) | 精读 of both papers at activation | SATURATED — CONFIRMED (2026-07-12). ELVIS: "uncertainty-based soft truncation… Adaptive-horizon MPC adapts effective return horizon online using real-time confidence" (§contrib 1-2); Neubay: "truncate rollouts adaptively using epistemic uncertainty as a threshold (Frauenknecht 2024; Zhan 2021)" — technique predates all 3. CAWM-H adaptive-horizon kill correct. | Brier 0.0225 (p=0.85, outcome 1) | KILL confirmed (consistent L5). Abstract-level triage nearly produced a FALSE doubt — Neubay's abstract stresses long-horizon, its method does truncation → 精读 mandatory even when the abstract seems to contradict the occupancy claim. |
| J4 | 2026-07-12 | SELECT | R2-Dreamer reproduces its 3-task Phase-1 minimum on our box without a blocking bug (i.e., TD-MPC2 fallback NOT needed as main) | p=0.60 {guess} | new repo ~8 commits vs strong venue + verified-by-download layout; L3 new-substrate prior | Phase-1 exit item 1 | YES — R2-Dreamer reproduces its 3-task minimum (2026-07-12): walker_walk 957.95 · cheetah_run 877.49 · reacher_subtle final 696.4 (last-8 mean ~726, max 936.9, target ~800 wide band per paper Fig5; baselines ~0-200). No blocking bug; TD-MPC2 fallback NOT needed as main. | Brier 0.16 (p=0.60, outcome 1) | New-repo BUILD reproduced clean on first contact — ICLR-2026 repo (~8 commits) worked out of the box (only TD-MPC2, the AUX repo, had env friction). This is the MEASURED evidence that lifts direction-survival above L1's 0.3 floor (L1 is about survival-to-method-WIN, not reproduce; a passed reproduce gate is exactly what raises it). 0.60 was appropriately cautious but slightly under-confident for a verified-by-download repo. |
| J5 | 2026-07-12 | COST | A 1M-step DMC-vision R2-Dreamer run on one 4090 lands in 12–30 wall-clock hours (point estimate ~20h) | p=0.60 the true value is in-range {guess} | no steps/sec measured anywhere; report gives VRAM only; L3 says first-contact estimates run low → range widened upward | 等级-0 timing probe (first vision run) | ~3h — WRONG (2026-07-12). Measured steady-state 99.98 steps/s on one 4090 (clean 60s delta, step 10016→16015); ETA to 1.01e6 ≈ 2.8h + ~10min compile/prefill ≈ 3.0h total. Actual ~3h is FAR BELOW the forecast 12-30h range (6.5× overestimate). | Brier 0.36 (p=0.60 in-range, outcome 0 — out of range low); log-ratio err ln(20/3.1)=1.86 | NEW PRIOR (→ distilled into L3): L3 (first-contact cost ×1.5-2, widen UP) applies to SETUP/DEBUG/download overrun, NOT steady-state GPU throughput — conflating them over-estimated a 3h run as 20h. Price training wall-clock ONLY from a measured steps/sec (always run the 60s probe); reserve L3's upward multiplier for env-build/first-run overhead. Whole Phase-1 gate is ~3h/task. |
| J7 | 2026-07-12 | PREREG | Week-1 headroom probe (calibrated-trigger SELECTIVE WM param-adaptation scheduling) survives all gates ⇒ warrants a method run | P(ACCEPT)=0.20 · effect ρ_trigger 0.3–0.5 @ ≤0.2·C_always · cost gates1-2 <1 GPU-day, gate-3 ~2-3 GPU-days | raw ~0.4 → L2 phenomenon≠recipe cap + stringent AND-gate (beats-yoked AND noninferior-to-always AND compute<0.2) + adaptation-false-negative risk on a frozen decoder-free WM; L1 feasibility | gate verdicts via /exp-verify (GATE-1 oracle-headroom first) | **KILL — slice too-marginal (2026-07-12).** R2-Dreamer arm VOID (RNG artifact, off-act-path module — L6). TD-MPC2 arm Δ_select WEAK-PASS (+0.71 LCB +0.13) but operator-strength sweep = CEILING (peaks at weakest operator, doesn't scale; prediction↔decision DECOUPLING). /autopsy region-closed. | Brier 0.04 (P(ACCEPT)=0.20, outcome 0=killed — well-calibrated: forecast low, died) | Forecast correctly LOW (L2 phenomenon≠recipe applied). Death mode = neither pre-named K3 nor GATE-1 but a THIRD: prediction gains don't route to decisions (dynamics-only adaptation, frozen-value bottleneck). Confirms L2 + the "flawed modular abstraction" critique. Cross-family independent review (GPT-5.6) was decisive TWICE — caught the R2D artifact AND priced the CEILING. → strengthens the invariant's value on surprising results (L6). |
| J8 | 2026-07-12 | SELECT | The RELIABILITY thread ("prediction-silent but decision-exploited value-staleness under dynamics-shift" — a decision-gap diagnostic that catches what prediction/uncertainty monitors provably miss) clears BOTH gates (occupancy-refutation + decision-gap dissociation probe) into a method run | P=0.35 {inferred} | two independent gates (J6-autopsy + D.2) converged on the same phenomenon (constructive, not eliminative) → shade UP; L1 feasibility + L4 OCCUPANCY-RISK is the real threat (model-exploitation MOPO/MOReL/MBPO is a crowded neighborhood) → shade DOWN. Reframe = user's (adaptation→reliability). | Gate 1 = GPT-5.6 occupancy/framing refutation (death→route Option-2 D.1 context); Gate 2 = decision-gap dissociation probe w/ §G#7+§G#6 controls + D.2 sign-disambiguation (death→route Option-3 reconsider WM). Self-resolving fork. | **Gate-1 RESOLVED (2026-07-12): CROWDED with a narrow OPEN seam** — not clean-kill, not clean-open. Broad phenomenon CLOSED (Edge-of-Reach, TRAP, Lambert, value-equivalence). KILLER incumbent = Filos model-value-inconsistency (+MOBILE). Seam OPEN only under a strict 6-part conjunction (oracle model/value factorial + restricted monitor-impossibility + planner-conditioned diagnostic that BEATS Filos/MOBILE). Mechanism corrected: model-value MISMATCH / finite-horizon-bootstrap, not "stale model". | P(ACCEPT) revised 0.35→~0.22 {the Filos cheapest-kill is the binding threat} | Bank `pro/gate1-occupancy-verdict-gpt56-2026-07-12.md`. NEXT = the Filos cheapest-kill on existing ckpts (catches→Option-2/small-paper; silent→run oracle factorial). Gate-2 design UPGRADED to GPT-5.6's factorial + Filos/MOBILE baselines + action-ranking-regret. **FINAL: KILLED at the crux (2026-07-12) — planner-conditioned D_rank AUROC lift −0.014 over pred+Filos in the prediction-silent stratum (perm_p 0.72, stable 8 seeds); phenomenon mild (pred⊥regret +0.19 not 0); no model-side signal predicts decision-regret. Brier 0.032 (P=0.18, outcome 0 — well-calibrated: forecast dropped 0.35→0.22→0.18 as gates fired, ended low, killed). → L7 phenomenon-magnitude gate. Region-closed; pivot routed via external brain.** |
| J9 | 2026-07-12 | SELECT | PIVOT (GPT-5.6 strategic): "decision-directed ACTIVE context acquisition" (probe to reduce planner-regret uncertainty, not prediction error) clears the frozen-checkpoint decision-BIFURCATION gate (5 magnitude criteria incl. active>passive) ⇒ first-order phenomenon worth a method | P=0.35 {inferred} | first-order claim (not a residual seam — fixes the 55% selection error diagnosed across J6-J8); exploits the negative result; DALI shows the context phenomenon is LARGE (+96.4%) → shade UP; but criterion (iv) active-BEATS-passive is the real risk (DALI's passive is strong) + L1 + stringent 5-criteria gate → shade DOWN | decision-bifurcation gate (G≥10%, bifurcation≥25%, probe recovers≥30%, active>passive≥5%, not-pathological); FAIL routes to substrate escalation (RWRL/RRLS→Meta-World) | **KILL (2026-07-12).** Decision-bifurcation gate: confound-control PASSED (G_long 7.5%/8.5% > G_short, genuine frozen-Q-independent context effect) BUT sub-threshold (<10%) + decision-INERT (BIF 0.98 but wrong-context regret tiny; oracle probe recovers ≈0% of G, active=passive at chance). ROOT: DMC locomotion goal is context-INVARIANT → shift makes goal harder not different → no decision lever. Brier 0.04 (P=0.20, outcome 0). | → **L8 substrate-goal-variance gate.** The context effect is real but not exploitable because the optimal policy doesn't differ by context. Points BACK to reacher_subtle (goal DOES vary per episode) as the right substrate. Confirms L2 (phenomenon≠exploitable). Data `evaluation/decision_bifurcation_gate/`. |
| J10 | 2026-07-12 | SELECT | DIRECTION (GPT-5.6-decided, node 4): "decision-utilization gap" — R2-Dreamer has target-conditioned control DEAD ZONES (target decodable yet deterministic actor fails) — clears the matched-state-sweep magnitude gate (≥15% region, ≥20-25% loss, DETERMINISTIC actor failure, ≥50% oracle-action recovery) ⇒ first real method candidate | P=0.45 {inferred} | first-order + own-anomaly-grounded + GPT-5.6-green-lit unoccupied seam + rough grid already shows systematic target-difficulty structure → shade UP; but stringent magnitude gate + the "actor-MODE removes the 543↔916 variance = unlucky sampling" kill (grid across-repeat var is HIGH: ±132/±427) is the real risk + L1 → shade DOWN | matched-state counterfactual target sweep (node 4.1) w/ magnitude gate + kill conditions; then regret-gated decision-head repair vs equal-compute-continued-training baseline | **KILL (2026-07-12, full run confirming).** Matched-state sweep: deterministic actor MODE near-oracle (G_actor 163.75 vs G_oracle 172.25 = 95%); mode_failures=0, frac_intrinsic_to_mode=0.0 (failures are stochastic SAMPLING, not the mode); no contiguous dead zone (blob 8% <15%); oracle-action rescue 18% (<50%). Phenomenon NOT real — reacher instability = actor action-sampling noise on a precision task. | Brier ~0.09 (P shaded 0.45→~0.30 after the lean-stochastic grid; outcome 0). | → L9 mode-vs-sample. Grid pre-look correctly forecast the sampling-noise kill. The GPT-5.6-green-lit DIRECTION died at PHENOMENON-verification (the seam was novel but the effect isn't there). bank `pro/DIRECTION-decision-utilization-gap-gpt56-2026-07-12.md`; grid (rough) conflates target-difficulty w/ arm-state — the MATCHED-STATE + actor-MODE sweep was decisive |

| J11 | 2026-07-12 | SELECT | REFRAMED DIRECTION (GPT-5.6 synthesis): the campaign = a falsification methodology (DMC = negative control, 6 nulls); the POSITIVE phenomenon = sparse action-ranking failures at CONTACT-MODE transitions in ManiSkill2 PickYCB (TD-MPC2, ~60% success) — clears the contact-conditioned oracle-reranking probe ⇒ the campaign’s first POSITIVE result | P=0.40 {inferred} | first checkpoint-available + non-occupied (causal localization) + the diagnostic is CALIBRATED (6 nulls) + real residual failure (40%) → UP; new-substrate (L3) + the ≥10pt/≥3× magnitude gate + “residual gap might be proposal/exploration/value not WM-dynamics” → DOWN | contact-conditioned oracle-reranking probe on the frozen PickYCB ckpt; setup = ManiSkill2 install + ckpt (moderate new resource, flagged) | **KILL (2026-07-13).** Node 5.1 region-closed after 3 estimand iterations (v3→v4→v5) + 2 cross-family reviews. Contact-onset localization IS REAL (5.08× peak/shoulder onset profile, 93.5% engaged episodes spike) but NOT per-event sparse-exploitable (0.438 < 0.50 frozen co-primary). Conversion law: decision bottleneck is DIFFUSE over engaged/failing trajectories, not localized-and-exploitable at contact boundaries — feeds node-4 convergence (7th signal that value/policy is the bottleneck, not dynamics). | Brier 0.13 (P=0.40, outcome 0) | Direction CLEARED setup (reproduce sub-gate PASS) but died at the phenomenon-form gate. The three-estimand chain (v3→v5) proved the value of FROZEN estimands (caught defects) but also proved estimands need a POSITIVITY CHECK alongside the decision rule (v4 was structurally blind to 31/37 episodes). → L8 positivity/MNAR. |
| J12 | 2026-07-12 | PREREG | The contact-conditioned oracle-reranking probe COMMITS: contact states ≥3× matched free-space action-ranking regret AND oracle-reranking at contact states lifts PickYCB success ≥10pts (frozen TD-MPC2, no training) | P(commit)=0.33 · pred contact/free regret ratio 2–5× · pred success lift 4–14pts · cost ≤3 GPU-h/phase after pilot | raw ~0.45: TD-MPC2 PLANS with its model at eval (dynamics DO route into selection) → the frozen-value wall (J7/J8/J10) is WEAKER here, UP; L2 cap (0.4) + the recurring “prediction gains don’t route to decisions” death + ≥10pt is a large bar + L3 → net 0.33 | `/exp-verify` on `evaluation/maniskill_contact_rerank/` (prereg `prereg/contact-mode-oracle-reranking-2026-07-12.md`) | **KILL (2026-07-13, terminal).** Prereg COMMIT gate failed across 3 iterations: v3 uninterpretable (LOO 0.36×–4.54×) → v4 DEFECTIVE (positivity/MNAR failure, episode-FE blind to 31/37 eps, codex refutation UPHELD) → v5 TERMINAL (3/4 clauses pass — onset peak/shoulder 5.08× PASS, engaged sparse 93.5% PASS — but co-primary per-event sparse-fraction 0.438 < 0.50 FAIL; codex adjudication UPHELD region-close). Existence confirmed but not in exploitable form. | Brier 0.07 (P(commit)=0.33, outcome 0 — well-calibrated low; the L2 phenomenon≠recipe cap was the binding prior) | resolves J11. Forecast well-calibrated (0.33 → died): L2 phenomenon≠recipe cap was the binding prior. The estimand-chain discipline (frozen + cross-family review) was essential — v4 would have been a false kill (positivity failure masked a real signal), v5’s terminal freeze honored a failing co-primary that tested exactly what it was meant to test. → L8 positivity/MNAR. |

| J3 | 2026-07-12 | SELECT | ≥1 of the 3 Week-1 frozen-checkpoint probes (exploitability-G · trigger-sweep · context inference-gap) clears its pre-registered bar | p=0.70 {inferred} | bank headroom analysis; DALI's reported +96.4% context gain suggests at least the context gap is real; raw instinct 0.8, L1 feasibility-optimism prior shades down | Week-1 probe verdicts (`/exp-verify`) | **KILL (2026-07-13, superseded).** 0/3 cleared. The three probes evolved as the campaign pivoted: exploitability-G → J7 (KILL, prediction↔decision decoupling), trigger-sweep subsumed by J6/J7 (KILL), context inference-gap → J9 (KILL, DMC context-invariant goal → decision-INERT). All died on the same root: DMC context-invariant goals → no decision lever. | Brier 0.49 (P=0.70, outcome 0 — badly over-confident; the OR of correlated probes ≠ the OR of independent ones) | Over-confidence on aggregate OR — all three probes shared the same binding root (DMC substrate mismatch, L7); pricing them at p=0.70 from individual ~0.45s assumed independence that did not hold. When probes share a SUBSTRATE, failure correlation is near-1. → Reinforces L7 (substrate pre-flight BEFORE pricing the aggregate). |

<!-- J2/J6 moved from the live ledger 2026-07-13: SUPERSEDED by the fresh WORLD MODELS restart
     (Arbor run `world-model-v2`, re-ideate from zero). These were open forecasts about the now-dead
     event-triggered re-anchoring direction; retained here as history, not carried into the fresh env. -->
| J2 | 2026-07-12 | OCCUPANCY | Event-triggered re-anchoring in learned latent WMs is OPEN as an AAAI slice | p=0.65 open at 精读; expect ~0.5 still-open by submission (AdaJEPA's future-work note invites others too) | audit D.3 + Trick.md Route-A convergence; AdaJEPA 2606.32026 unread (L4) | 精读 2606.32026 + fresh occupancy search at activation; RE-RESOLVE at submission time | OPEN at 精读 — CONFIRMED (2026-07-12); SUPERSEDED by fresh restart 2026-07-13. AdaJEPA adapts/replans EVERY MPC step (unconditional), NOT event-triggered → "when to re-anchor" unoccupied; AdaJEPA = the always-adapt incumbent to beat. | Brier 0.1225 (open-at-精读 sub-forecast p=0.65, outcome 1); submission re-resolution VOIDED by restart | Decision-relevant differential (banked for reuse if the seam is re-prospected): event-triggered re-anchoring MATCHES AdaJEPA's always-adapt return at a fraction of the adaptation compute, and/or avoids benign-transition over-adaptation. |
| J6 | 2026-07-12 | OCCUPANCY | Naive "event-triggered replanning in latent WMs" PRE-EMPTED (DOWN); refined "calibrated-trigger scheduling of WM PARAMETER-update + joint {no-op/re-anchor/replan/adapt} triage, decision-centric" stays OPEN (narrow) | naive p(open)→0.15; refined p(open)≈0.55 {inferred, decays to submission} | GPT-5.6 occupancy map + LOCAL 精读: AdaReP 2606.23079 (replanning CLOSED); ARCADE 2512.14331; WISER 2512.01119. L4 satisfied by local read. Bank `openbuild/world-model/pro/occupancy-search-event-triggered-2026-07-12.md` | RE-RESOLVE at submission — VOIDED by restart | naive DOWN CONFIRMED (AdaReP real & strict); refined OPEN — SUPERSEDED by fresh restart 2026-07-13 | — | Independent substrate narrowed the slice BEFORE compute committed. Incumbents to beat: AdaJEPA (always-adapt), ARCADE (continuous changepoint), AdaReP (replan-only). Cross-ref J2. |
