<!-- Paste everything below the `---` after `/goal`. Body is <3000 chars. -->
---
AMBITION (the ONE goal): produce ONE genuine 一区-grade contribution (Information Fusion / ESWA tier). The DIRECTION — topic, problem, idea, method, TYPE — is deliberately NOT prescribed; research-os SELF-SELECTS it. Don't ask which-direction — decide and report; the human sets the ambition + value bar, never the direction.

MODE = FAST (性价比): a cheap probe SIZES the prize before any big run; fewest runs to a decision; kill fast. /rigor + full ablation fire ONLY at the confirmation/venue boundary, never per-candidate — quality per unit time, not perfection.

VALUE BAR (the only hard core — what keeps "get a paper" honest): the result must be a GENUINE, falsifiable contribution of an honestly-named research TYPE (§5.0), defensible at a 一区 bar — NOT a safe certificate / audit-theater / measurement-retreat. **ANTI-RETREAT:** when a direction is refuted, do a PROGRESSIVE problemshift (a new attack predicting a NEW fact), never a retreat to the safest publishable thing. A diagnosis/evaluation endpoint is allowed ONLY if a build is proven unreachable — /compass flags it a retreat, not progress. Contribution is human-granted (the one invariant).

AUTONOMY: research-os v0.9 is mature — RUN it. /prospect (own-log anomalies + taste-bank operators FIRST — operators = a 切入, not only /forge fuel) → /forge (+ forced off-frame + forced taste-operator) → /rigor (FAST, confirmation-only — NOT per-candidate) → /prereg (launch-arithmetic + armed Monitor; resume-fail = STOP) → run → /exp-verify → /adversary (opposing baseline OPTIMIZED to win; ≥3 seeds, paired) → /autopsy → /compass (every 2nd). Human redirects from REPORTS, not the direction.

REALITY (constraints, not direction): compute = 2×4090D — frozen VLM + LoRA/adapter + heavy eval, NOT pretraining / SOTA-race / from-scratch. Built asset: StarVLA (frozen Qwen2.5-VL-3B + FAST + LoRA) + LIBERO-Plus — use if /prospect picks VLA, NOT mandated (state in §0). CLOSED, do NOT cross-contaminate: DSpark × speculative decoding + the dLLM/DiffusionGemma/LLaDA campaign (separate, non-active).

ENGINES (§1): Pro generates (design/off-frame/operator, `Pro 扩展` KEEP ALIVE); Opus operates; Codex AUTO HOOK.

READ: operating-manual (§0 state · §1 engines · §4.8-10 launch-arithmetic/Monitor · §5 types+discipline+generator) → campaign docs + RUNLOG if continuing. Keys .env.

RULES (§5.1): name the TYPE first, verify BY type; /prereg seals the contract for any claim-bearing run; NUMBERS FROM ARTIFACTS (§4.8); DOWN-only self-grant, CLAIM_STANDS independent, contribution → human.

STOP: /compass STOP_AND_REPORT · a contribution clears /adversary at the 一区 bar → human · caps hit · a direction refuted AND no progressive problemshift remains ⇒ /prospect LATERAL (a move, never idle).

WAIT/PARK (§4.9): to wait (train/eval) or PARK → a Monitor or Bash bg-wait (ONE clean signal), NEVER a ScheduleWakeup poll; browser→Pro = quiet 15-min checks.
