---
name: prospect
description: Hunt problems worth an experiment from five mines, OWN-LOG ANOMALIES FIRST (anomaly ledger · survey-as-problem-mine · capability deltas · benchmark critique · transplants) + taste-operator retrieval as a prior → 3–7 taste-ranked problem cards {Q, TYPE, WHY-NOW, STAKES, PROBE, SURPRISE} + a frame/operator ledger. Loads the `prospect` skill. $ARGUMENTS = the corpus / territory pointer.
disable-model-invocation: true
argument-hint: "[corpus or territory pointer]"
---
Run the `prospect` skill on: $ARGUMENTS
