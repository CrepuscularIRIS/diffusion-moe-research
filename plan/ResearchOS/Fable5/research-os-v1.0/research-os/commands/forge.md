---
name: forge
description: Turn ONE problem card into a live, taste-ranked backlog of 3–5 candidates {MECHANISM, KILL, COST, SURPRISE, FRAME, DIFF-PREDICTION} — one forced rival-SCHOOL candidate, one forced non-incumbent-FRAME candidate (with DIFF-PREDICTION or discard), one forced rotated OPERATOR candidate — plus the REGENERATION RULE so the backlog never becomes a menu. Loads the `forge` skill. $ARGUMENTS = the problem card or tree-node pointer.
disable-model-invocation: true
argument-hint: "[problem card or tree-node pointer]"
---
Run the `forge` skill on: $ARGUMENTS
