---
name: autopsy
description: Convert every null, failed run, killed candidate, or DOWN verdict into search-space information — boring-first triage → mechanism-level why → DOWN-only scope grade → THE CONVERSION LAW (constraint | candidate | region-close) + [ANOMALY]/[OPERATOR-CANDIDATE] ledgers + the programme pulse (surprise accounting · type-drift · Lakatos · process budget · diversity). Loads the `autopsy` skill. $ARGUMENTS = the dead run/candidate + artifacts.
disable-model-invocation: true
argument-hint: "[the null/failed run or killed direction + artifacts]"
---
Run the `autopsy` skill on: $ARGUMENTS
