---
name: adversary
description: The single claim-boundary gate — fires ONLY when a claim leaves the loop. Four TYPE-scoped checks: Δ-reality · baseline fairness (make the OPPOSING baseline win) · claim–evidence map (no artifact ⇒ hypothesis; claim ≤ allowed envelope; memory ≠ evidence) · worth. DOWN verdicts self-administrable; CLAIM_STANDS = independent substrate only; contribution = human only. Loads the `adversary` skill. $ARGUMENTS = the claim + run/prereg pointers.
disable-model-invocation: true
argument-hint: "[the claim + run artifacts + prereg pointer]"
---
Run the `adversary` skill on: $ARGUMENTS
