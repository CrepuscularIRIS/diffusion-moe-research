---
name: prereg
description: Freeze the evidence contract BEFORE any claim-bearing run — {HYPOTHESIS, MECHANISM, TYPE, METRIC, sealed SPLIT, UNIT+TEST, ACCEPT/KILL, NEG-CONTROL, SEEDS, ONE-VAR; + ablations/baselines/allowed-claim for a confirmatory run} in 10–20 lines, timestamped via Arbor. Post-hoc edits void the run as evidence; exploration stays free. Loads the `prereg` skill. $ARGUMENTS = the chosen forge card.
disable-model-invocation: true
argument-hint: "[the chosen forge card]"
---
Run the `prereg` skill on: $ARGUMENTS
